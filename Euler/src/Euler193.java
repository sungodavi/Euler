import java.util.*;
public class Euler193 
{
	public ArrayList<Integer> sieve;
	public static int recurse(long num, int index)
	{
		int count = 1;
		for(int i = index + 1; i < sieve.size(); i++)
		{
			count += rec
		}
	}
}
